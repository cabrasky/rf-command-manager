# RF Command Manager

A Home Assistant companion for managing Broadlink RF and IR commands from one place.

The core idea is to give users a simple remote-control dashboard where they can learn commands, organize them by device, rename them as needed, and send them quickly from a clean interface.

It should feel like a virtual remote that keeps the user’s command library easy to browse and maintain.

Create a side pannel app to controll it...
